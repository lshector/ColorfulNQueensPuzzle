import { MenuControls }from "./menu_controls.js";

export class UploadMenuControls extends MenuControls {
    constructor(puzzleGrid) {
        super(puzzleGrid, 'upload-menu');
        // Add upload-specific logic here if needed
    }
}